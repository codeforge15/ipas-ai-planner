---
layout: default
title: 科目一｜人工智慧基礎概論
permalink: /subject-1/
---

# 科目一｜人工智慧基礎概論 學習筆記

> 標記說明：⭐ = 模擬題直接考過或極易出題｜⚠️ = 易混淆／原文有誤

---

# 3.1 人工智慧概念

## 1. AI 的定義與分類

**定義**：模擬人類智慧的技術，使機器能執行原本需人類智慧才能完成的任務——**學習、推理、解決問題、感知環境**。

**近十年發展突飛猛進的原因**：軟硬體技術進步、計算能力提升、開放資料普及、數據的多樣性/品質/規模增長、演算法與機器學習精進。

⭐ **依功能分三類**（最常考的分類法）：

| 類型 | 目的 | 典型場景 |
|---|---|---|
| 分析型 AI | 洞悉數據模式，分析處理大量數據 | 提供有價值見解 |
| 預測型 AI | 基於歷史資料預測未來趨勢與行為 | 市場預測、風險評估 |
| 生成型 AI | 依使用者提示詞（prompt）生成素材 | 文字、語音、圖像、影片 |

⭐ **AI 不等於深度學習**：AI 是廣義領域，涵蓋機器學習、深度學習、專家系統、NLP、電腦視覺等（模擬題考過兩次，答案都是「AI 包括各種技術，例如機器學習、專家系統等」）。

**企業應用四大目標**：提高效率、增強決策能力、提供個人化服務、促進創新。生成式 AI 自 **2022 年**起快速發展。

## 2. 五大應用領域

| 領域 | 三個代表應用 |
|---|---|
| 醫療保健 | 疾病診斷（X 光/MRI 影像）、藥物研發（分子結構模擬）、個人化醫療 |
| 金融 | 風險評估（信用記錄）、欺詐檢測（即時監控異常）、自動交易（高頻） |
| 製造業 | 自動化生產、品質控制（影像辨識檢測缺陷）、預測性維護 |
| 交通 | 自動駕駛、交通流量預測 |
| 娛樂 | 遊戲開發、虛擬現實、內容推薦 |

## 3. AI 三層架構

```
技術底層  →  開發應用  →  實際運用
```

**（1）技術底層 5 要素** ⭐

| 要素 | 內容 | 關鍵字 |
|---|---|---|
| 資料處理與分析 | 清理、整合、儲存、分析 | ETL、資料庫管理、Hadoop、Spark |
| 演算法 | 解決問題與決策支援的邏輯基石 | 迴歸分析、分類演算法、決策樹、基因演算法 |
| 機器學習 | 用資料訓練模型做預測或分類 | 監督式、非監督式、強化學習 |
| 深度學習 | 建於人工神經網路，處理非結構化數據 | 語音辨識、影像處理、NLP；TensorFlow、PyTorch |
| 專家系統 | 基於**規則與知識庫**模擬專家決策 | 醫療診斷、財務分析 |

**（2）開發應用**：模型設計、訓練、測試、部署。
**（3）實際運用**：設計行業解決方案（智慧醫療/物流/製造）、打造產品與服務（語音助理、推薦演算法）、優化業務流程。

## 4. 資料處理與分析（四步驟）

```
數據蒐集 → 數據清洗 → 數據轉換 → 數據分析
```

### （1）數據蒐集

⭐ **結構三型**（必考）：

| 類型 | 特徵 | 例子 |
|---|---|---|
| 結構化 | 清晰固定結構，行列形式 | 關聯式資料庫 MySQL、PostgreSQL、規範化電子表格 |
| 半結構化 | 有結構標籤但格式靈活，不需嚴格架構 | **XML、JSON、CSV** |
| 非結構化 | 無固定結構，需處理解析後才能分析 | 圖片、影像、音訊、電子郵件、文章 |

**蒐集方法**：問卷與調查、自有產品數據（網站/App/智慧手錶）、外部公開數據（政府開放平台 API、網路爬蟲）、外部付費購買、網路爬蟲。

### （2）數據清洗（四類問題）⭐

| 問題 | 定義 | 處理方式 |
|---|---|---|
| 遺缺值 Missing | 欄位無有效數據 | 填補（平均/中位數/眾數、插補法、迴歸預測）或刪除記錄 |
| 重複值 Duplicate | 相同內容記錄 | 查主鍵/唯一碼識別 → 保留一份刪其餘 |
| 錯誤值 Error | 不符合理範圍或明顯錯誤 | 修正。例：年齡 −5 歲、Taiwan 拼成 Taiwwn |
| 離群值 Outlier | 遠離多數數據點的異常值 | 依業務目標決定策略 |

⚠️ **離群值 ≠ 錯誤值**：原文明講「離群值通常**可能是有意義的數據**而非錯誤值」，可能反映真實異常情況，也可能是雜訊。這是最容易被出成陷阱題的一句。

### （3）數據轉換（五種）

| 轉換 | 說明 | 例子 |
|---|---|---|
| 格式轉換 | 一種格式→另一種 | CSV → JSON |
| 類型轉換 | 資料型別改變 | 字串 → 數值 |
| 正規化／標準化 | 縮放到特定範圍，消除單位影響 | [0,1] 或 −1~1 |
| 離散化 | 連續型 → 離散區間/類別 | 年齡 → 青年/中年/老年 |
| 數據縮減 | 特徵選擇、特徵提取、降維 | **PCA** |

### （4）數據分析四型 ⭐⭐（本節最高頻考點）

| 類型 | 目的 | 代表方法 |
|---|---|---|
| **敘述性** Descriptive | 描述基本特徵與分布 | 平均值、中位數、標準差、百分位數；直方圖、散佈圖、折線圖 |
| **探索性** Exploratory | 無預設假設，多角度找模式/關聯/異常 | 散佈圖矩陣、熱圖、平行坐標圖、箱型圖、相關性分析（Pearson）、聚類（K-Means/階層）、PCA、異常檢測 |
| **診斷性** Diagnostic | 探究現象的**根本原因** | 鑽取分析、關聯分析（Apriori、「啤酒與尿布」）、因果分析（因果圖、A/B 測試） |
| **預測性** Predictive | 用歷史數據預測未來 | 迴歸模型、分類模型（決策樹/隨機森林/SVM/深度學習）、時間序列（ARIMA/SARIMA/LSTM）、集成學習（RF/GBM/XGBoost） |

**敘述性統計圖表優缺點**（易出「何者不適合…」）：

| 工具 | 優點 | 缺點 |
|---|---|---|
| 平均值 | 簡單易懂 | **受極端值影響大** |
| 中位數 | 不受極端值影響 | 對分布變動性不敏感 |
| 標準差 | 量化分散程度，可跨組比較 | 計算複雜、受極端值影響 |
| 百分位數 | 詳細描述分布、不受極端值 | 計算量大 |
| 直方圖 | 直觀展示分布（集中、分散、異常） | **組間隔選擇影響外觀** |
| 散佈圖 | 顯示兩變量相關性、找異常值 | 點過多會擁擠 |
| 折線圖 | 展示隨時間變化趨勢 | 不適合類別型數據 |

⭐ **雷達圖不適合估計資料分布**——它是比較「多個個體在多維度上的表現」，不呈現機率密度。要看分布用直方圖。

## 5. 演算法

**搜尋與排序四種**：

| 演算法 | 機制 |
|---|---|
| 線性搜尋 | 從第一個元素逐個比較 |
| 二分搜尋 | 與中間元素比較，往左半或右半繼續 |
| DFS 深度優先 | 沿一條路徑走到底，再回溯 |
| BFS 廣度優先 | 一層一層向外擴散 |

**三個常見演算法** ⭐：

| 演算法 | 本質 | 重點 |
|---|---|---|
| 線性迴歸 | **迴歸**（連續數值） | 找最佳直線/超平面；優：簡單高效；缺：**只能捕捉線性關係** |
| 邏輯迴歸 | ⚠️ **名為迴歸，實為分類** | Sigmoid 把輸出壓到 0~1 機率，閾值（如 0.5）分類；缺：只解二元分類，多類別需擴展 |
| KNN | 基於實例 | 找最近的 K 個鄰居；分類取多數類、迴歸取平均或中位數 |

## 6. 機器學習（概述）

**三步驟**：① 準備訓練資料（蒐集、過濾雜訊、前處理）② 訓練模型（輸入演算法、調參）③ 測試及評估（反覆訓練，仍不佳則換模型重來）。

**三類學習方法**（詳見 3.3）：

| 類型 | 資料 | 任務 | 例子 |
|---|---|---|---|
| 監督式 | **有標記** | 分類、迴歸 | 垃圾郵件過濾、影像識別、房價預測 |
| 非監督式 | 無標記 | 聚類、降維、特徵提取 | 市場區隔、異常偵測、推薦系統、影像壓縮 |
| 強化學習 | 環境回饋 | 序列決策 | ⭐ 圍棋、自動駕駛、機器人控制、遊戲 AI |

⭐ 「動態、重複互動、需試錯與長期規劃」→ 一律選**強化學習**。

## 7. 深度學習三大經典模型 ⭐⭐

**共通特性**：多層神經網路自動學特徵，**不需人類專家手動設計特徵**（與傳統 ML 最大差別）。

### CNN 卷積神經網路
- 靈感來自動物視覺皮層，擅長**圖像**
- 三層結構：**卷積層**（提取局部特徵，如邊緣紋理）→ **池化層**（降維，最大/平均池化，防過擬合）→ **全連接層**（映射到輸出做分類/迴歸）
- 優點：參數量遠少於全連接網路；能提取層次化特徵（低層邊緣 → 高層物體形狀）
- 應用：人臉辨識、MNIST、物件偵測、醫學影像分割

### RNN 循環神經網路
- 有**循環連接**，記憶前一時刻狀態，適合**序列/時間**資料
- 改進版：**LSTM**（遺忘門 forget、輸入門 input、輸出門 output → 解梯度消失）、**GRU**（比 LSTM 簡單，效果相近）
- 缺點：梯度消失/爆炸、長序列計算效率低
- 應用：機器翻譯、文本生成、語音轉文字、股價預測

### GAN 生成對抗網路
- 兩個網路：**生成器 Generator**（接收隨機雜訊產生假數據，目標「欺騙」判別器）＋ **判別器 Discriminator**（分辨真假）
- 缺點：訓練不穩定、**模式崩潰 Mode Collapse**、需大量算力
- 應用：圖像生成（草圖→寫實）、文本生成、語音合成

## 8. 生成式 AI 的原理與訓練

**運作原理** ⭐：依輸入提示詞，在大量文本訓練建立的「**文字機率分布**」中尋找**最可能接續的文字**，逐字生成。參數越多 → 越能捕捉語言細微差異。

**兩階段**：

| 階段 | 內容 |
|---|---|
| **訓練階段** | ① 數據準備（蒐集/清洗/預處理→轉數字向量）② 模型選擇與搭建（GAN、VAE、Transformer；輸入層/隱藏層/輸出層）③ 模型訓練（損失函數、優化器 Adam/SGD、迭代至收斂） |
| **微調階段** | ① 特定任務優化（調學習率、批量大小等超參數；添加特定層）② 數據精調（特定資料集、資料增強如旋轉翻轉提升泛化） |

⭐ **影響生成效果四因素**：數據品質、模型結構、超參數設置、計算資源。

## 📌 3.1 模擬題考點對照

| 題 | 答案 | 考的是 |
|---|---|---|
| 1 | D | 圍棋/自駕 → 強化學習 |
| 2、9 | B | AI 涵蓋多種技術，不限深度學習 |
| 3 | B | 透明性 → 發布報告/技術文件/網站揭露 |
| 4 | C | 雷達圖不適合分布估計 |
| 5 | C | K-Means 的 K = 分群數量 |
| 6 | D | 增加正則化項降低過擬合 |
| 7 | C | 生成式 AI 核心 = 生成新的原創內容 |
| 8 | B | 神經網路是生成式 AI 的基石 |
| 10 | D | AI 治理國際合作：統一標準＋避免濫用＋促進技術轉移 |

---

# 3.2 資料處理與分析概念

## 1. 統計測量數三類

**中央趨勢** ⭐⭐

| 測量數 | 定義 | 優點 | 缺點／限制 |
|---|---|---|---|
| 平均數 Mean | 總和 ÷ 個數（算術平均數最常用；另有幾何、調和） | 計算簡單、易理解、可做數學運算 | **易受極端值影響** |
| 中位數 Median | 排序後最中間的值（偶數取中間兩數平均） | **不受極端值影響**，反映典型水平 | 難做複雜數學運算與統計推斷 |
| 眾數 Mode | 出現頻率最高的值 | 不受極端值，直接反映最常見類別 | **可能有多個，也可能不存在** |

⭐ **適用時機**（模擬題考過）：
- 房價含少數豪宅極端高價 → 用**中位數**
- 某產品銷量遠高於其他 → 用**眾數**

⭐ **偏態方向**（考過兩次，一定要背）：
- 平均數 > 中位數 → **正偏態**（右偏，尾巴向右）
- 平均數 < 中位數 → **負偏態**（左偏，尾巴向左）
- 平均≈中位≈眾數 → 對稱分布

**分散度**

| 測量數 | 定義 | 特性 |
|---|---|---|
| 四分位數 Quartile | 排序分四等份的三個分割點 | |
| 全距 Range | 最大 − 最小 | 只看兩端，**受極端值影響**，不能精確反映全體 |
| 四分位距 IQR | **Q3 − Q1**，代表中間 50% 範圍 | 原文稱「與全距有一樣的缺點」，但題目答案採「IQR = Q3−Q1」 |
| 平均差 | 各觀察值與平均數的差距 | 數值越大分散程度越高 |
| 標準差 SD | 衡量分散程度 | ⭐ **品管中標準差大 → 過程波動大 → 品質不穩定、良率低** |

⚠️ 原文寫「變異數（Coefficient of Variation）」中英不符：CV 是**變異係數**，變異數是 Variance。

## 2. 抽樣與機率模型

樣本統計量與母體參數的差異 = **抽樣變異** → 引入**機率模型**量化不確定性 → 進行**區間估計**與**假設檢定**。

## 3. 統計方法分類

| 無因果關係 | 有因果關係 |
|---|---|
| 敘述性統計、複選題分析、（多重）交叉分析、卡方檢定、主成份分析、因素分析、集群分析、（偏）相關分析 | 二元羅吉斯迴歸、區別分析、偏相關分析、迴歸分析、（多變量）變異數分析、偏最小平方迴歸 |

## 4. 假說檢定 ⭐⭐

**流程**：猜想（假定）→ 蒐集資料 → 檢定 → 作決策（接受或拒絕）

| 名詞 | 定義 |
|---|---|
| 虛無假設 H0 | 表示**不存在**顯著效果或差異，檢定的基準假設 |
| 對立假設 Ha（H1, K） | 與 H0 相對，表示存在顯著效果或差異 |
| 簡單假設 | 完全確定母體分布的**所有**參數 |
| 複合假設 | 僅對**部分**參數作假設 |
| 接受域 | 使 H0 被接受的集合 |
| 拒絕域 | 使 H0 被拒絕的集合 |
| 檢定統計量 T(X) | 樣本的實值函數，用來決定是否接受 H0 |
| 顯著水準 α | 又稱檢定大小，常取 0.1 / 0.05 / 0.01 |
| p 值 | 在 H0 為真下，統計量落在比觀測值至少同樣極端區域的機率；又稱觀測大小 |

**兩類錯誤**：

| | H0 為真 | H0 為假 |
|---|---|---|
| **接受 H0** | 正確 | **Type II 錯誤（β）** |
| **拒絕 H0** | **Type I 錯誤（α）** | 正確 |

- α = P(拒絕 H0 ∣ H0 為真)
- β = P(接受 H0 ∣ Ha 為真)

⚠️ 原文 3-27 表格把「拒絕 H0 ∣ H0 為真」誤植為 Type II，記定義即可，別被表格帶偏。

⭐ **判準與解讀**：p < α → 拒絕 H0。例：p=0.03、α=0.05 → 拒絕 H0，**「有 95% 的信心」**（不是 97%）。α=0.05 代表願意承擔 5% 犯 Type I 錯誤的風險。

## 5. EDA vs CDA ⭐

| | 探索性 EDA | 驗證性 CDA |
|---|---|---|
| 時機 | 研究初期，對資料不熟悉 | 已有假設之後 |
| 目的 | **無預設假設**，發現模式/關係/異常，**生成假設** | **驗證**先前提出的假設並深入挖掘 |
| 方法 | 視覺化探索 | 分類、分群、相關性分析、預測模型（可結合定性分析） |

## 📌 3.2 模擬題考點對照

| 題 | 答案 | 考的是 |
|---|---|---|
| 1 | D | K-means **不能**處理類別型資料 |
| 2 | B | 標準差大 → 生產波動大、品質不穩 |
| 3 | B | ⚠️ 官方答 B，但依常理 A（原理複雜）才不是 k-means 特點——記「k-means 原理簡單」 |
| 4 | B | CDA 重在驗證假設 |
| 5 | B | 有極端值 → 中位數 |
| 6 | C | 平均 < 中位數 → 負偏態 |
| 7 | B | p=0.03 < α=0.05 → 95% 信心拒絕 H0 |
| 8 | C | 平均 > 中位數 → 正偏態 |
| 9 | C | IQR = Q3 − Q1 |
| 10 | B | 單一產品銷量遠高 → 眾數 |

> ⚠️ **本科評鑑主題含「資料隱私與安全」，但指引無內文。**請改讀《筆記_科目二》3.3 節「風險管理」段落（訓練數據洩漏、反向工程、提示詞攻擊、對抗性攻擊、差分隱私、GDPR/CCPA、匿名化/假名化），該段可直接作為本主題教材。

---

# 3.3 機器學習概念

## 1. 基本原理

機器學習＝**數據驅動**，讓系統從經驗中學習、**無需人為編寫規則**，自動改進效能。特徵是**基於數據的學習與自適應能力**：分析歷史數據建預測模型，新數據到來時動態更新。

**技術優勢**：不同於傳統編程；能處理大規模結構化＋非結構化數據（圖像/文本/語音）；可擴展性高。

**挑戰**：數據品質（缺失、不平衡、雜訊）、**解釋性與透明性不足**（限制在醫療、法律的應用）、隱私與倫理、偏見導致歧視性輸出。
**未來方向**：提升解釋性與公平性（視覺化、可解釋演算法、去偏演算法）、跨領域整合。

## 2. 三大類型

### （1）監督式學習
以**有標記資料**學習輸入→輸出的映射。

| 任務 | 代表演算法 | 場景 |
|---|---|---|
| 迴歸 | 線性迴歸 | 房價預測（面積、地理位置） |
| 分類 | **SVM**：找最大化類別間隔的**超平面**；線性不可分時用**核函數**映射到高維 | 醫學影像判斷腫瘤良性/惡性 |
| 分類/迴歸 | 神經網路 | 語音辨識（音訊→文字） |

### （2）非監督式學習
無標記，找內在結構。核心兩目標：**聚類**與**降維**。

| 目標 | 演算法 | 場景 |
|---|---|---|
| 聚類 | **K-Means** | 市場區隔、客戶分群、網路安全異常檢測 |
| 降維 | **PCA**（線性變換壓縮到低維同時保留主要資訊） | 圖像壓縮、基因數據分析 |

⭐ **K-Means 特性**：K = 希望分成的群組數；隨機選初始中心 → **不同初始中心可能得到不同結果**；依距離計算、各群質心為代表點；**易受雜訊與離群值影響**；**不適合非球形、密度變化大、有離群值、類別型資料**。

### （3）強化學習

**三核心要素**：
- **代理 Agent**：做決策與行動的學習系統
- **環境 Environment**：依 Agent 行動提供回饋
- **獎勵 Reward**：行動好壞的回饋訊號

**流程四步**：初始狀態 → 選擇行動（策略可為隨機 **Exploration** 或依經驗 **Exploitation**）→ 執行行動（環境返回新狀態與獎勵）→ 更新策略（最大化**累積獎勵**）

| 方法 | 重點 |
|---|---|
| Q-Learning | 學每個行動的**長期價值（Q 值）**，試錯找最佳決策 |
| Deep Q-Learning (DQN) | 狀態空間太大時 Q-Table 不可行 → 用 **DNN 近似 Q 值**；輸入狀態、輸出各行動 Q 值；引入**經驗回放 Experience Replay** 與**目標網路 Target Network** 穩定訓練 |

**應用**：AlphaGo（自我對弈）、Atari 遊戲、機器人步態控制、機械臂抓取、自駕路徑規劃。

## 3. 三階段流程 ⭐⭐

```
數據處理 → 模型訓練 → 模型評估與優化
```

### （1）數據處理

**清洗**：均值填補（數值型）／中位數或眾數填補（偏態或類別型）／刪除法（缺失比例過高）。
**異常值檢測**：**四分位距法**、**標準差法**。

**標準化**（⭐ 公式要記）：

| 方法 | 公式 | 結果 |
|---|---|---|
| Min-Max Scaling | (X − Xmin) / (Xmax − Xmin) | 映射到 [0, 1] |
| Z-score | (X − μ) / σ | 均值 0、標準差 1 |

> 在使用**梯度下降等對特徵尺度敏感**的演算法時特別重要。

**特徵選擇三法** ⭐：

| 方法 | 機制 | 例子 |
|---|---|---|
| 過濾法 Filter | 用相關性或統計量篩選 | Pearson 相關係數 |
| 包裝法 Wrapper | 用模型效能評估特徵組合 | RFE 遞迴特徵消除 |
| 嵌入法 Embedded | 模型內建機制自動篩選 | LASSO 正則化 |

**降維**：PCA。

### （2）模型訓練

**損失函數 Loss Function**：衡量預測值與實際值差異，損失越高越不準；透過**優化器**用**梯度下降**最小化損失。

| 損失函數 | 用於 |
|---|---|
| **MSE 均方誤差** | 迴歸 |
| **Cross-Entropy 交叉熵** | 分類 |

**優化演算法**：

| 演算法 | 特性 |
|---|---|
| BGD 批次梯度下降 | 用**整個資料集**算梯度 |
| SGD 隨機梯度下降 | 每次**一個樣本**，快但收斂不穩定 |
| **Adam** | 結合動量法 + RMSProp，**自適應學習率**，目前最廣泛使用 |

⭐ **過擬合防範三招**：**Regularization**（L1/L2 懲罰項）、**Early Stopping**（驗證集表現下降就停）、**Data Augmentation**（旋轉翻轉擴增資料）。

> 過擬合定義：訓練集表現優異但測試集差 → 原因是**模型過度學習了訓練數據中的雜訊或特定模式**。

### （3）模型評估與優化

| 指標 | 適用 |
|---|---|
| Accuracy 準確率 | **平衡的**分類問題 |
| F1 Score（精確率 Precision + 召回率 Recall） | **數據不平衡**問題 |
| MSE | 迴歸問題 |

**交叉驗證**：**K-Fold** — 分成 K 個子集，每次一個當測試集其餘訓練，重複 K 次取平均。目的是**降低過擬合風險、提升泛化能力、評估穩健性**。

**調參三法**：網格搜索 Grid Search（逐一嘗試）／隨機搜索 Random Search（適合高維參數空間）／**貝葉斯優化**（建代理模型依歷史結果逐步尋優）。

## 📌 3.3 模擬題考點對照

| 題 | 答案 | 考的是 |
|---|---|---|
| 1 | A | ⭐ ML 三核心要素 = **數據、模型、損失函數** |
| 2 | A | 監督式 = 資料集含標記 |
| 3 | C | 過擬合 = 過度學習雜訊 |
| 4 | C | 交叉驗證 → 減少過擬合風險 |
| 5 | B | 梯度下降 → 優化參數以最小化損失函數 |
| 6 | B | 線性迴歸 → 銷售額預測（連續值） |
| 7 | B | 決策樹最大優勢 = **可解釋性** |
| 8 | B | 神經網路 = 多層結構學複雜特徵 |
| 9 | B | GAN = 生成器 + 鑑別器 |
| 10 | B | 隨機森林 = 集成多棵樹並投票 |

---

# 3.4 鑑別式 AI 與生成式 AI 概念

## 1. 核心差異（本節命脈）⭐⭐⭐

| | 鑑別式 AI (DAI) | 生成式 AI (GAI) |
|---|---|---|
| **學什麼機率** | **條件機率 P(y\|x)** | **聯合分布 P(x,y) 或邊際分布 P(x)** |
| **目標** | 找**分類邊界**（Boundary Determination），做分類/迴歸 | 模擬真實數據分布，**生成新樣本** |
| **輸出** | 分類標籤或數值預測 | 新的數據樣本（文字、圖像、音訊） |
| **同輸入同輸出？** | 是，決定性 | 否，**相同輸入產生不同輸出**（固有變異性 Inherent Variance） |
| **代表模型** | 邏輯迴歸、SVM、決策樹、隨機森林、神經網路 | GAN、VAE、Diffusion Models |
| **挑戰** | 數據偏見、過擬合、**標記成本** | 內容真實性、**可控性**、計算成本 |

**流程對比圖（3-49 頁，純圖片，文字層抽不到）**：
- 鑑別式：**資料 → 邊界判定 → 分類／預測（決策）**
- 生成式：**提示詞 → 輸入並反覆調整 → 創建 → 生成內容**

> 使用者會基於**提示工程（Prompt Engineering）**持續反覆調整輸入直到任務解決——這正是生成式有別於鑑別式的關鍵。

## 2. 鑑別式 AI 五模型

| 模型 | 原理 | 應用 |
|---|---|---|
| **邏輯迴歸** | 假設線性關係，**Sigmoid** 映射到 0~1，用**最大似然估計 MLE** 學參數，超過閾值（0.5）歸類 | 醫療診斷、市場營銷轉換率、金融違約風險 |
| **SVM** | 找**最大化類別間隔的超平面**；不可分時用**核函數**（多項式核、高斯核）映射高維 | 文本分類（垃圾郵件、情感分析）、圖像識別、生物資訊學 |
| **決策樹** | **遞迴分割**；用**資訊增益／吉尼係數／均方誤差**選最佳劃分特徵；節點=特徵條件，葉節點=類別 | 醫療診斷、金融風險、行銷分析 |
| **隨機森林** | **Bootstrap 取樣**建多棵獨立樹 → **投票或平均**；引入隨機性降低過擬合 | 醫學影像分類、金融市場分析、生態監測 |
| **神經網路** | 輸入層/隱藏層/輸出層；**激勵函數 + 反向傳播**調整權重最小化損失 | 語音辨識、自動駕駛、醫學影像 |

## 3. 生成式 AI 三模型

| 模型 | 原理 | 應用 |
|---|---|---|
| **GAN** | 生成器（雜訊→假數據，目標欺騙）vs 鑑別器（辨真假），對抗學習共同進步 | StyleGAN 人臉、圖像修復、**風格遷移**、醫療影像生成、虛擬場景 |
| **VAE** | **編碼器**壓縮到低維**隱變量空間 Latent Space** → **解碼器**重建；引入隨機變量與正則化 | 數據修復重建、**異常檢測**（學正常分布找異常）、數據生成、醫療數據增強 |
| **Diffusion 擴散模型** | 訓練階段**逐步加雜訊**至高斯分布；生成階段**反向去雜訊**重建 | 高解析度圖像生成、醫療影像合成、**文本到圖像** |

## 4. 整合應用

**（1）價值**
- **數據增強與分析協同**：GAN 生成稀缺病變影像 → 供鑑別式模型訓練 → 提高辨識腫瘤能力與**泛化效能**；金融模擬市場情境供風險評估
- **多模態處理**：生成式負責跨模態生成轉換，鑑別式負責精確分類決策。例：自駕生成各種天氣/光線/路況虛擬場景供學習；智慧語音互動（生成式產生回應、鑑別式做語音辨識與語意分析）
- **提升泛化能力**：教育（模擬學生行為數據）、智慧城市（模擬突發事件生成應急數據）

**（2）技術優勢**
- 數據生成與判斷融合：GAN 生成病理影像 → CNN 訓練分類；工業/資安異常檢測
- 即時分析與回饋：自駕（生成濃霧冰雪場景 + 鑑別式即時識別規劃路徑）、網路安全（生成攻擊模式 + 鑑別式即時分析流量）
- 靈活性與適應性：智慧客服（生成式產生回應、鑑別式過濾不當內容）、內容生成與審核

**（3）挑戰與解方** ⭐

| 挑戰 | 解方 |
|---|---|
| 模型訓練穩定性（模式崩潰、梯度消失） | 採用 **Wasserstein GAN (WGAN)**、自適應優化技術 |
| 數據偏差與公平性 | 生成過程引入**去偏演算法**、強化數據審核 |
| 整合架構設計 | **分層架構**分別處理生成與分類 + **共享層**即時交換資訊 + 動態學習框架 |

## 📌 3.4 模擬題考點對照

| 題 | 答案 | 考的是 |
|---|---|---|
| 1 | C | 鑑別式目標 = 分類或迴歸 |
| 2 | C | GAN 屬生成式（SVM/LR/RF 皆鑑別式） |
| 3 | C | 分類醫學影像**不是**生成式應用 |
| 4 | C | GAN 兩組件 = 生成器 + 鑑別器 |
| 5 | B | 鑑別式學 P(y\|x)，專注分類/迴歸 |
| 6 | C | 醫學影像整合目的 = 提高準確性與數據多樣性 |
| 7 | A | 模擬交通場景訓練自駕 = 生成式支援鑑別式 |
| 8 | B | 訓練不穩定 → WGAN |
| 9 | B | 數據增強解決**數據稀缺或不平衡** |
| 10 | B | 未來關鍵 = 開發更高效的**整合框架** |

---

# 附：本科評鑑主題涵蓋度自檢

- [x] AI 的定義與分類 → 3.1
- [ ] **AI 治理概念 → 指引無內文**，需外部補強（治理與可信任 AI 專題）
- [x] 資料基本概念與來源 → 3.1 §4
- [x] 資料整理與分析流程 → 3.1 §4 + 3.2
- [ ] **資料隱私與安全 → 指引無內文**，借用科目二 3.3 風險管理段補足
- [x] 機器學習基本原理 → 3.3
- [x] 常見的機器學習模型 → 3.1 §5 + 3.3 + 3.4
- [x] 鑑別式與生成式基本原理 → 3.4 §1
- [x] 鑑別式與生成式整合應用 → 3.4 §4
